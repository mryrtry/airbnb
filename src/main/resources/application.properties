spring.application.name=airbnb
spring.jpa.open-in-view=false

# По умолчанию профиль dev — при запуске без -Dspring.profiles.active подгружается application-dev.properties (JWT, БД, почта).
spring.profiles.active=dev